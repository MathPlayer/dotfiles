# Don't let ripgrep show really long lines to terminal; show a preview instead.
--max-columns=150
--max-columns-preview

# Search case-insensitive if pattern is all lowercase.
--smart-case

# Search in hidden folders. Note: gitignore'd folders are skipped.
--hidden

# Filter cucumber feature files
--type-add
cucumber:*.feature

# Exclude the '.git' folders since they're not ignored by gitignore :)
--glob
!.git/*
